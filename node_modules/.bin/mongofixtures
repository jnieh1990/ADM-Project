#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pow-mongodb-fixtures/bin/mongofixtures" "$@"
  ret=$?
else 
  node  "$basedir/../pow-mongodb-fixtures/bin/mongofixtures" "$@"
  ret=$?
fi
exit $ret
